{
  "name": "trustless-casino-provider",
  "version": "1.0.0",
  "private": true,
  "description": "Trustless casino provider on TON with provably fair mechanism, slashing, and verification",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "server": "ts-node server/src/index.ts",
    "dev": "ts-node server/src/index.ts",
    "lint": "tsc --noEmit",
    "compile": "npx tact --config tact.config.json",
    "deploy": "npm run compile && ts-node scripts/deploy.ts",
    "demo": "ts-node scripts/demo.ts",
    "generate-wallet": "ts-node scripts/generate-wallet.ts"
  },
  "devDependencies": {
    "@tact-lang/compiler": "^1.6.13",
    "@ton/blueprint": "^0.45.0",
    "@types/cors": "^2.8.0",
    "@types/express": "^4.17.0",
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "cors": "^2.8.0",
    "dotenv": "^16.4.0",
    "express": "^4.18.0",
    "ton": "^13.9.0",
    "ton-core": "^0.53.0",
    "ton-crypto": "^3.2.0"
  }
}
